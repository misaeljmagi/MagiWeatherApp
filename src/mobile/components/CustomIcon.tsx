import React from 'react';
import {Icon, IconProps} from 'react-native-elements';

const CustomIcon: React.FC<IconProps> = props => (
  <Icon tvParallaxProperties {...props} />
);

export default CustomIcon;
